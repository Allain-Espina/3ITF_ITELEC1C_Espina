@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}
@model IEnumerable<EspinaITELEC1C.Models.InstructorModel>

<h1>Instructor List</h1>

<a class="btn btn-success" asp-action="AddInstructor">Add Instructor</a>

<table class="table table-hover table-sm">
    <tr>
        <th hidden>Instructor ID:</th>
        <th></th>
        <th>First Name:</th>
        <th></th>
        <th>Last Name:</th>
        <th></th>
        <th>Status:</th>
        <th></th>
        <th>Rank:</th>
        <th></th>
        <th>Date Hired:</th>
        <th></th>
        <th>Show Details:</th>
        <th></th>
        <th>Update:</th>
        <th></th>
        <th>Delete:</th>
    </tr>

    @foreach (var instructor in Model)
    {
        <tr>
            <td hidden>@instructor.Id</td>
            <td></td>
            <td>@instructor.FirstName</td>
            <td></td>
            <td>@instructor.LastName</td>
            <td></td>
            <td>
                @{
                    var status = "";
                    if (@instructor.IsTenured)
                    {
                        status = "Full-Time Employee";
                    }
                    else
                    {
                        status = "Part-Time Employee";
                    }
                }
                @status
            </td>
            <td></td>
            <td>@instructor.Rank</td>
            <td></td>
            <td>
                @{
                    var DateHired = @instructor.HiringDate;
                    string HiringDate = String.Format("{0:dd/MM/yyyy}", DateHired);
                }
                @HiringDate
            </td>
            <td></td>
            <td><a style="color: white;" class="btn btn-primary" asp-action="ShowDetails" asp-route-id="@instructor.Id">Show Details</a></td>
            <td></td>
            <td><a style="color: white;" class="btn btn-info" asp-action="UpdateInstructor" asp-route-id="@instructor.Id">Update</a></td>
            <td></td>
            <td><a style="color: white;" class="btn btn-danger" asp-action="DeleteInstructor" asp-route-id="@instructor.Id">Delete</a></td>
        </tr>
    }
</table>